{
  "name": "controle-vendas-root",
  "version": "1.0.0",
  "description": "Sistema de Gestão de Vendas Parceladas e Crediário VIP",
  "scripts": {
    "docker:up": "docker-compose up -d --build",
    "docker:down": "docker-compose down",
    "install:all": "cmd /c \"cd server && npm install && cd ../client && npm install\"",
    "start:server": "cmd /c \"cd server && npm start\"",
    "start:client": "cmd /c \"cd client && npm run dev\""
  }
}
